import 'package:flutter/material.dart';

class MyPracticeView extends StatelessWidget {
  const MyPracticeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('My Practice'),
      ),
    );
  }
}
